// En aquest cas el meu company i jo hem arribat a la conclusió que la forma més simple de representar un racional és utilitzar dos enters, un pel numerador (nume) i l'altre pel denominador (den)
int nume;
int den;

// Funció estàtica per calcular el màxim comú divisor (mcd) de dos nombres enters
static int mcd(int a, int b);
// Funció estàtica per calcular el mínim comú múltiple (mcm) de dos nombres enters
static int mcm(int a, int b);
